import { createContext } from "react";

export const DeploymentsContext = createContext()

export const DeploymentsContextProvider = () => {

    return(
        <DeploymentsContext.Provider>
            
        </DeploymentsContext.Provider>
    )
}